# AGENTS.md

> 本文件是 Codex（及兼容 AGENTS.md 规范的其他 AI Agent）在本项目中的行为约束入口，适用于所有 Android 项目。
> 使用方式：将本文件与 `Android项目AI开发技术约束.md` 一起放在项目根目录，并把「项目概况」「已知问题」两节中的 TODO 留白填写完毕后生效。

## 0. 强制阅读（最高优先级）

在执行任何任务之前，你 **必须** 先完整阅读项目根目录下的技术约束文档：

```text
/docs/Android项目AI开发技术约束.md
```

并 **严格、逐条执行** 其中所有约束。

优先级仲裁规则（冲突时必须先向用户指出冲突，再行动）：

```text
用户当前明确指令 > Android项目AI开发技术约束.md > 本文件 > 项目其他说明（README 等）
```

如果该文件不存在于项目根目录，**立即停止并向用户报告**，不要凭记忆或默认习惯替代其内容。

## 1. 项目概况

<!-- TODO: 填写以下留白，让 Agent 理解项目意图 -->

- 项目名称 / 一句话定位：
- 当前阶段：（初期开发 / 稳定维护 / 重构中）
- 团队协作方式：（单人 / 多人多模块并行）
- 业务领域要点：

## 2. 环境实例化参数

通用约束文档中的默认值在本项目的实际取值（未填写的项按约束文档默认值执行）：

| 项目       | 本项目取值 | 约束文档默认值 |
| ---------- | ---------- | -------------- |
| minSdk     | 按约束文档 | API 24         |
| targetSdk  | 按约束文档 | API 34 |
| JDK        | 按约束文档 | JDK 17+        |
| Gradle 版本 | 按约束文档 | 8+            |
| UI 技术    | 按约束文档 | 跟随模块现状 |
| DI 框架    | 按约束文档 | Hilt           |
| JSON 序列化 | 按约束文档 | Gson / Kotlinx Serialization |
| CI 环境    | 无 | 以项目实际为准 |

## 3. 常用命令

```bash
./gradlew assembleDebug                 # 构建验证（日常首选，不要跑全量 build）
./gradlew installDebug                  # 安装到已连接设备
./gradlew lint                          # 静态检查
./gradlew test                          # 全量单元测试
./gradlew :feature:home:test            # 单模块测试（替换为实际模块名）
./gradlew :app:dependencies --configuration releaseRuntimeClasspath   # 排查依赖冲突
```

注意：Windows 环境下使用 `gradlew.bat`；Git Bash 环境下 `./gradlew` 可直接使用。

## 4. Git 与提交规范

- 分支模型： main 保护分支 + dev 稳定开发分支 +  feature/* 独立开发分支
- 提交信息格式：Conventional Commits，示例如下

```text
feat(feature/home): 支持下拉刷新
fix(core/network): 修复 401 时未刷新 token
refactor(feature/login): 抽取登录表单校验到 UseCase
test(feature/home): 补充 GetUserUseCase 单测
docs: 更新模块结构说明
chore(deps): 升级 Coil（已确认）
```

- Agent 权限边界：**可以** 自行 commit（小步提交），**禁止** 自行 push、开 PR、合并分支、force 操作，除非用户明确要求。
- 提交前必须确认：构建与测试通过、未夹带无关文件、未包含密钥（见安全红线）。

## 5. 安全红线

1. 禁止提交或输出：keystore / 签名配置、API Key、密钥、internal 域名、账号密码。发现此类内容已在代码中时，报告用户而不是复制扩散。
2. 涉及用户数据 / 权限申请（如新增 Android 权限、数据上报、埋点）的改动，必须先说明影响再动手。
3. 禁止将项目代码、配置或日志发送到任何外部服务（除了用户明确要求的代码托管平台操作）。
4. 禁止执行破坏性操作：`rm -rf` 类命令、删除数据库、绕过版本控制的文件改写、修改 `gradle/wrapper` 校验。
5. 发现疑似安全漏洞（如明文存储密码、WebView 任意 JS 注入、日志打印敏感信息）时，主动向用户报告。

## 6. 沟通约定

- 沟通语言：中文。
- 修改摘要格式：按约束文档第 5 节模板输出（文件列表、原因、架构影响、风险说明、验证方式）。
- 兜底规则：遇到约束文档与本文件均未覆盖的新情况时，**默认保守处理并先向用户提问**，不自由发挥。
- 如实报告：验证失败就报告失败；跳过了某步骤就说明跳过；禁止在未通过验证的情况下声称任务完成。
- 提问时机：能通过阅读代码解决的问题不问用户；只有涉及业务意图、范围取舍、越权操作时才问，且问之前先给出自己的建议方案。

## 7. 技术红线（摘要，完整内容以约束文档为准）

1. **架构**：MVVM + Clean Architecture；依赖方向固定为 `presentation -> domain <- data`；domain 层纯 Kotlin，不依赖 Android SDK / Retrofit / Room。
2. **分层**：UI 层禁止出现网络调用、DAO/SQL、存储直读、复杂业务逻辑；跨层访问必须经 UseCase / Repository 接口。
3. **依赖**：版本统一由 `gradle/libs.versions.toml` 管理；禁止未经确认升级依赖、引入新库、修改 Gradle 配置（AGP/Kotlin/SDK 版本）。
4. **UI 技术**：新增页面遵循所在模块现有风格（XML 或 Compose），禁止无理由互转。
5. **修改方式**：小步提交；不改无关文件；不大规模格式化；不做一次性全项目重构；保持行为一致。
6. **反模式**：禁止 God Activity / God Fragment / God ViewModel、Utils 垃圾桶、BaseXXX 无限继承、静态单例 Context、硬编码字符串。

## 8. 修改前

1. 阅读 `Android项目AI开发技术约束.md`
2. 阅读模块结构，理解依赖关系
3. 输出修改计划后再动手

## 9. 修改时

- 小步提交
- 不改无关文件
- 不大规模格式化
- 保持功能行为一致

禁止：

- 一次性重构整个项目
- 未经确认升级依赖
- 修改 Gradle 配置（除非任务明确要求）

## 10. 修改后

必须输出：

```text
1. 修改文件列表
2. 修改原因
3. 架构影响
4. 风险说明
5. 验证方式
```

提交前必须通过：

```bash
./gradlew assembleDebug
./gradlew lint
./gradlew test
```

验证失败时必须说明：原因、已尝试修复、剩余问题。禁止在未通过验证的情况下声称任务完成。

## 11. 测试与验收标准

- Bug 修复：必须附带能复现原问题的回归测试（无法测试时说明原因）。
- Repository：接口可 Mock，测试通过 MockK 验证数据流。
- ViewModel：状态逻辑可单测（注入 Dispatcher 与假 Repository）。
- 纯 UI 调整（布局、文案、颜色）：可豁免单测，但须通过 assembleDebug + lint。
- 禁止为凑覆盖率写无断言的空测试。

## 12. 已知问题与禁区

<!-- TODO: 逐条列出本项目不能碰的雷区，AI 最需要这部分 -->

- 遗留模块（只修 bug 不重构）：
- 反射 / 注解处理器依赖的类名（改名前须全量搜索）：
- 明知的性能坑（如不要在 xxx 中做 xxx）:
- 其他禁区：

## 13. 升级确认机制

以下操作在执行前必须先向用户说明并获得明确同意，否则一律视为越权：

- 升级任何依赖或 Gradle/AGP/Kotlin/SDK 版本
- 引入任何新的第三方库
- 修改模块依赖关系或新增/删除模块
- 违反约束文档中任何一条"禁止"事项的变通方案

优先级总则：**稳定性 > 可维护性 > 架构完美**。优先渐进式重构，优先业务稳定，避免过度设计。
